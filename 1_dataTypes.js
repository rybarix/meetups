/**
    Six data types that are primitives:
      boolean
      null
      undefined
      number
      string
      symbol (new in ECMAScript 2015) https://www.keithcirkel.co.uk/metaprogramming-in-es6-symbols/
      object
 */

const b = true || false;

const n = null

const num = number(100);

const str = 'Hello mada...';
const str2 = "faka...";

const sym1 = Symbol('description'); // https://www.keithcirkel.co.uk/metaprogramming-in-es6-symbols/
